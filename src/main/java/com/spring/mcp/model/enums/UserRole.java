package com.spring.mcp.model.enums;

/**
 * Enumeration of user roles for the management UI.
 */
public enum UserRole {
    /**
     * Administrator with full access
     */
    ADMIN,

    /**
     * View-only access (read-only)
     */
    VIEWER
}
